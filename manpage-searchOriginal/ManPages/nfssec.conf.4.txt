<beginning of page>
nfssec.conf <-> list NFS security modes
/etc/nfssec.conf
The nfssec.conf file lists the NFS security modes supported on a system. These modes are defined in nfssec(5).
The nfssec.conf file should not be edited by a user.
nfssec(5)
